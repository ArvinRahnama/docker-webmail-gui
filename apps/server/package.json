{
  "name": "@dwg/server",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "Web tier (Fastify): auth, sessions, SQLite, audit, jobs. Serves the SPA. Holds no Docker socket and no Docker vocabulary — see ARCHITECTURE.md §2.",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "build": "tsc --build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@dwg/shared": "^0.1.0",
    "@fastify/cookie": "^11.0.1",
    "@fastify/helmet": "^13.0.0",
    "@fastify/multipart": "^9.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@fastify/static": "^8.0.3",
    "@node-rs/argon2": "^2.0.2",
    "fastify": "^5.2.0",
    "pino": "^9.6.0",
    "tar": "^7.4.3",
    "undici": "^7.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/tar": "^6.1.13"
  }
}
